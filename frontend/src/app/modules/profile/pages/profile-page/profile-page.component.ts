import { Component, OnInit, Renderer2 } from '@angular/core';

@Component({
  selector: 'app-profile-page',
  templateUrl: './profile-page.component.html',
  styleUrls: ['./profile-page.component.scss']
})
export class ProfilePageComponent {
/*
  constructor(private renderer: Renderer2) { }

  ngOnInit(): void {
    // Ajoutez le lien CSS Bootstrap
    const linkCSS = this.renderer.createElement('link');
    this.renderer.setAttribute(linkCSS, 'rel', 'stylesheet');
    this.renderer.setAttribute(linkCSS, 'href', '//maxcdn.bootstrapcdn.com/bootstrap/4.1.1/css/bootstrap.min.css');
    this.renderer.setAttribute(linkCSS, 'id', 'bootstrap-css');
    this.renderer.appendChild(document.head, linkCSS);
  
    // Ajoutez le script Bootstrap
    const scriptBootstrap = this.renderer.createElement('script');
    this.renderer.setAttribute(scriptBootstrap, 'src', '//maxcdn.bootstrapcdn.com/bootstrap/4.1.1/js/bootstrap.min.js');
    this.renderer.appendChild(document.body, scriptBootstrap);
  
    // Ajoutez le script jQuery
    const scriptJQuery = this.renderer.createElement('script');
    this.renderer.setAttribute(scriptJQuery, 'src', '//cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js');
    this.renderer.appendChild(document.body, scriptJQuery);
  }
  
*/
}
